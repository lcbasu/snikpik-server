security:
  cookie-props:
    domain: ${DOMAIN}
  allow-credentials: true
  allowed-origins:
    - https://${DOMAIN}
    - https://${SERVER_DOMAIN}
    - https://${CONSOLE_DOMAIN}
  allowed-origin-patterns:
    - https://*.${vercel.app}
